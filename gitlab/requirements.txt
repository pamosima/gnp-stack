# Python deps for Ansible runner (NetBox inventory + Cisco IOS collect_configs).
# ansible-core: brings in jinja2, PyYAML, etc.
# paramiko: SSH/network_cli when ansible-pylibssh not present.
# pynetbox + requests: NetBox API (nb_inventory).
# netaddr: used by netbox.netbox / ansible.utils for IP handling.
# pytz: required by netbox.netbox nb_inventory plugin.

ansible-core==2.18.1
paramiko>=3.0.0
pynetbox>=7.0.0
requests>=2.28.0
netaddr>=1.3.0
pytz>=2023.3
