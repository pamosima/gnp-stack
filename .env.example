# Copy to .env and set for your deployment.
# Used by compose (e.g. HYPERDX_APP_URL) and as the canonical host for docs/scripts.

# Host where gnp-stack runs (IP or hostname). Devices send syslog/gNMI/IPFIX here.
GNP_STACK_HOST=198.18.134.22

# Optional: full URL for HyperDX UI when not on default port (e.g. when using 8081).
# HYPERDX_APP_URL=http://198.18.134.22:8081
